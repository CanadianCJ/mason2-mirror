$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_tryRoot = $PSScriptRoot
if ([string]::$null
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_tryRoot)) {
  try { Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_tryRoot = Split-Path -Path $MyInvocation.MyCommand.Path -Parent } catch { Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_tryRoot = "$env:USERPROFILE\Desktop\Mason2\tools" }
}
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_lib = Join-Path (Split-Path Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_tryRoot -Parent) 'lib\Mason.Base.psm1'
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}_lib -Force
$Base = (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent)
$Rep  = Join-Path $Base 'reports'
$snap1= Join-Path $Rep 'urlacl_snapshot.txt'
$snap2= Join-Path $Rep 'servicestate_snapshot.txt'
$a = (netsh http show urlacl) | Out-String
$b = (netsh http show servicestate) | Out-String
if(Test-Path $snap1){ $old = Get-Content $snap1 -Raw } else { $old = '' }
if($a -ne $old){
  Out-MasonJsonl -Kind 'urlacl' -Event 'changed' -Level 'WARN'
  $a | Set-Content $snap1
}
if(Test-Path $snap2){ $old2 = Get-Content $snap2 -Raw } else { $old2 = '' }
if($b -ne $old2){
  Out-MasonJsonl -Kind 'servicestate' -Event 'changed' -Level 'WARN'
  $b | Set-Content $snap2
}