param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_tryRoot = $PSScriptRoot
if ([string]::IsNullOrWhiteSpace(Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$MasonBase = Get-MasonBase -FromPath $PSScriptRoot
Set-Location $MasonBaseImport-Module "$env:USERPROFILE\Desktop\Mason2\lib\Mason.Logging.psm1" -Force
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_tryRoot)) {
  try { Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$MasonBase = Get-MasonBase -FromPath $PSScriptRoot
Set-Location $MasonBaseImport-Module "$env:USERPROFILE\Desktop\Mason2\lib\Mason.Logging.psm1" -Force
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_tryRoot = Split-Path -Path $MyInvocation.MyCommand.Path -Parent } catch { Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$MasonBase = Get-MasonBase -FromPath $PSScriptRoot
Set-Location $MasonBaseImport-Module "$env:USERPROFILE\Desktop\Mason2\lib\Mason.Logging.psm1" -Force
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_tryRoot = "$env:USERPROFILE\Desktop\Mason2\tools" }
}
Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$MasonBase = Get-MasonBase -FromPath $PSScriptRoot
Set-Location $MasonBaseImport-Module "$env:USERPROFILE\Desktop\Mason2\lib\Mason.Logging.psm1" -Force
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_lib = Join-Path (Split-Path Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$MasonBase = Get-MasonBase -FromPath $PSScriptRoot
Set-Location $MasonBaseImport-Module "$env:USERPROFILE\Desktop\Mason2\lib\Mason.Logging.psm1" -Force
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_tryRoot -Parent) 'lib\Mason.Base.psm1'
Import-Module Import-Module (Join-Path (Split-Path $PSScriptRoot -Parent) 'lib\Mason.Base.psm1') -Force
$MasonBase = Get-MasonBase -FromPath $PSScriptRoot
Set-Location $MasonBaseImport-Module "$env:USERPROFILE\Desktop\Mason2\lib\Mason.Logging.psm1" -Force
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
_lib -Force
$MasonBase = Get-MasonBase
Set-Location $MasonBase
param([int]$N=5)
for($i=0;$i -lt $N;$i++){
  Out-MasonCoalesced -Key 'demo.error.X' -WindowSec 60 -OnEmit {
    param($count)
    Out-MasonJsonl -Kind 'demo' -Event 'error_X' -Level 'WARN' -Data @{ occurrences = $count }
  }
  Start-Sleep -Milliseconds 200
}
